[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = "*"
flask-cors = "*"
pytest = "*"

[dev-packages]

[requires]
python_version = "3.10"

[scripts]
app = "python app.py"
test = "pytest ."
coverage = "pytest --cov-report term-missing --cov=."
